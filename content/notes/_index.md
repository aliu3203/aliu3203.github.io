---
title: Notes
type: landing

# ---------------------------------------------------------------------------
# Adding a note
#
#   1. Copy `content/notes/template/` to `content/notes/<slug>/`
#   2. Drop the PDF into that folder, next to `index.md`
#   3. Fill in the front matter and delete the `draft`/`build` lines
#
# Notes are grouped by their `course:` field. A note with no `course:` lands in
# the "General" group at the bottom. Group order follows the most recently
# dated note in each group; pin it explicitly with `course_order:` below.
# ---------------------------------------------------------------------------

sections:
  - block: notes
    id: notes
    content:
      title: Notes
      section: notes
      text_from: 'snippets/notes-intro'
      # course_order:
      #   - 'CS 180'
      #   - 'PHYS 115A'
---

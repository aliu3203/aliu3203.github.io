---
title: 'Notes intro'
# Headless: this file is a text snippet, not a page. It is pulled into the
# Notes blocks via `text_from: snippets/notes-intro`, and is never rendered at
# a URL of its own or listed anywhere.
build:
  render: never
  list: never
---

These are a collection of notes I've written for classes I've taken so far. They are by no means exhaustive and will contain typos.
